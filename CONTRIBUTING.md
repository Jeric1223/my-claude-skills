# 기여하기

이 저장소는 Claude Code에서 반복적으로 쓰는 워크플로우를 재사용 가능한 스킬로 정리해두는 곳이다. 다른 프로젝트에서도 통하는 스킬이라면 PR로 추가해도 좋다.

## 스킬을 추가하기 전에

먼저 이 스킬이 정말 재사용 가능한지 확인한다. 다음 중 하나라도 아니라면 이 저장소에는 맞지 않는다.

- **처음에 바로 안 떠올랐던 절차인가** — 이미 다들 아는 표준 관행이면 스킬로 만들 이유가 없다
- **여러 프로젝트에 걸쳐 다시 쓸 만한가** — 특정 회사·특정 프로젝트에서만 통하는 규칙은 그 프로젝트의 `CLAUDE.md`에 넣는 게 맞다
- **트리거 조건이 구체적으로 설명 가능한가** — "언제 이 스킬을 써야 하는지"가 애매하면 다른 스킬과 충돌하거나 아예 호출되지 않는다

## 폴더 구조

`README.md`의 "스킬 구조" 섹션과 동일하다.

```
skill-name/
  SKILL.md       # 필수 — 트리거 조건, 절차, 흔한 실수 (에이전트가 읽는 파일)
  template.md    # 선택 — 출력 형식이 고정되어야 하는 경우의 스켈레톤
  README.md      # 필수 — 사람이 읽는 설명: 뭘 하는지, 왜 이렇게 설계했는지, 사용 흐름
```

`SKILL.md`와 `README.md`의 역할을 섞지 않는다. `SKILL.md`는 에이전트가 실행 중에 참조하는 절차 문서, `README.md`는 저장소를 훑어보는 사람을 위한 설명이다.

`SKILL.md` 프런트매터는 최소 `name`, `description` 두 필드가 필요하다. `description`은 "언제 쓰는지"만 담고, 절차 요약은 넣지 않는다 — 절차 요약이 들어가면 에이전트가 본문을 안 읽고 요약만 보고 판단하는 경우가 생긴다.

## PR 프로세스

1. 이 저장소를 fork하거나 브랜치를 딴다 (저장소에 write 권한이 없으면 fork)
2. `skill-name/` 폴더를 위 구조대로 작성한다
3. 루트 `.claude-plugin/marketplace.json`의 `plugins` 배열에 항목을 추가한다 (`name`, `source`는 필수)
4. 저장소 루트 `README.md`의 "스킬 목록" 표에 새 스킬 행을 추가한다
5. `claude plugin marketplace add .` 로 로컬 등록 후 `claude plugin install <name>@jeric-skills`가 성공하는지 확인한다
6. PR을 연다 — 템플릿의 체크리스트를 채운다
7. 리뷰를 기다린다

## 리뷰 기준

- 위 "스킬을 추가하기 전에" 세 가지 질문에 답할 수 있는가
- `marketplace.json`에 항목이 등록되어 있고, 실제로 설치가 되는가
- `SKILL.md`의 `description`이 트리거 조건만 담고 있는가 (절차 요약 아님)
- 다른 사람이 그대로 갖다 써도 프로젝트 특수 정보가 섞여 들어가지 않는가
- `template.md`가 있다면, 실제로 출력 형식이 고정되어야 하는 스킬인가 (아니면 불필요한 파일)

지금은 사람이 직접 리뷰한다. 기여가 늘어나면 GitHub Actions로 파일 구조·frontmatter 형식 자동 검증을 추가할 계획이다.
